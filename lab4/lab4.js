function getWeekDay(date) {
    var days = ['неділя', 'понеділок', 'вівторок', 'середа', 'четверг', 'пятниця', 'субота'];

    return days[date];
}
function getMouthDay(date) {
    var days = ['січень', 'лютий', 'березень', 'квітень', 'травень', 'червень', 'субота'];

    return days[date];
}
